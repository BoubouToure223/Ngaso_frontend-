import 'package:flutter/material.dart';

class ProHomePage extends StatelessWidget {
  const ProHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Accueil (Pro)')),
    );
  }
}
