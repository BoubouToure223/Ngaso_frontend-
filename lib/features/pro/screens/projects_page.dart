import 'package:flutter/material.dart';

class ProProjectsPage extends StatelessWidget {
  const ProProjectsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Projet (Pro)')),
    );
  }
}
