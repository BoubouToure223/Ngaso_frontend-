
import 'package:flutter/material.dart';

class ProSignupPage extends StatelessWidget {
  const ProSignupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Professional Signup'),
      ),
      body: const Center(
        child: Text('This is the professional signup page.'),
      ),
    );
  }
}
