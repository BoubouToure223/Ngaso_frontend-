import 'package:flutter/material.dart';

class NoviceProjectsPage extends StatelessWidget {
  const NoviceProjectsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Projet (Novice)')),
    );
  }
}
